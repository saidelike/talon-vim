# this assumes this change in ~/.bashrc in wsl
# case "$TERM" in
# xterm*|rxvt*)
    # PS1="\[\e]0;${debian_chroot:+($debian_chroot)}wsl: \w\a\]$PS1"
win.title:/TERM:wsl/
-
tag(): user.vim_terminal_wsl